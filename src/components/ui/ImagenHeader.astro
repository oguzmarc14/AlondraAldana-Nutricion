---
import BotonHeader from "./BotonHeader.astro";

interface Props {
  h1Title: string;
  h2Title: string;
  imageSrc: string;
  imageAlt: string;
}

const { h1Title, h2Title, imageSrc, imageAlt } = Astro.props;
---

<div class:list={["relative", "flex", "h-screen", "w-full", "overflow-hidden"]}>
  <img
    src={imageSrc}
    alt={imageAlt}
    class:list={["absolute", "w-full", "h-screen", "flex"]}
  />
  <div
    class:list={[
      "relative",
      "flex",
      "h-screen",
      "sm:left-30",
      "flex-col",
      "justify-center",
      "gap-5",
      "left-10"
    ]}
  >
    <h1 class:list={["font-bold", "text-lg", "w-3/7", "sm:font-semibold", "text-brown/90", "mt-8", "sm:mt-0", "md:text-5xl lg:text-5xl sm:text-4xl"]}>{h1Title}</h1>
    <h2 class:list={["w-3/7", "text-brown/80", "sm:text-2xl", "text-sm md:text-3xl lg:text-4xl", , "-mt-4"]}>
      {h2Title}
    </h2>
    <slot>
      <div class:list={["flex", "sm:flex-row", "gap-2", "flex-col"]}>
      <BotonHeader
        botonTexto="Agenda tu cita"
        hrefBoton="Mis Servicios"
        botonPrimario={true}
      />
      <BotonHeader
        botonTexto="Servicios"
        hrefBoton="/servicios"
        botonPrimario={false}
      />
      </div>
    </slot>
  </div>
</div>
