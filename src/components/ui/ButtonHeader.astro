---
interface Props {
  buttonText1: string;
  buttonText2: string;
  numWhatsapp: string;
  href1: string;
}

const { buttonText1, buttonText2, numWhatsapp, href1 } = Astro.props;
---

<div>
  <a href={numWhatsapp}>{buttonText1}</a>
  <a href={href1}>{buttonText2}</a>
</div>
